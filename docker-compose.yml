version: "3.8"
services:
  paddle-ocr-api:
    image: paddle-ocr-api
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: ["gpu"]